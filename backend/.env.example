# === Required ===
GEMINI_API_KEY=               # Google AI Studio API key

# === Database (leave empty to use local SQLite for dev) ===
DATABASE_URL=

# === File Storage (leave empty to use local filesystem for dev) ===
CLOUDINARY_URL=

# === Optional ===
MAX_FILE_SIZE_MB=10
CORS_ORIGINS=http://localhost:3000
